# fastruments

**fastruments** is a Python library developed within the **FAST Group** to provide a high-level interface for controlling and acquiring data from laboratory instruments.

---

## Installation

Clone the repository and install in editable mode:

```bash
pip install git+https://github.com/ricalbr/fastruments.git
```

